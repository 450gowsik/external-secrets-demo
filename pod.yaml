apiVersion: v1
kind: Pod

metadata:
  name: test

spec:
  containers:
  - name: nginx
    image: nginx

    env:
    - name: USERNAME
      valueFrom:
        secretKeyRef:
          name: app-secret
          key: username