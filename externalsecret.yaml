apiVersion: external-secrets.io/v1
kind: ExternalSecret

metadata:
  name: app-secret

spec:
  refreshInterval: 1h

  secretStoreRef:
    name: demo-store
    kind: SecretStore

  target:
    name: app-secret

  data:
    - secretKey: username

      remoteRef:
        key: username